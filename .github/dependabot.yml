# Keep GitHub Actions and bats test submodules current.
# mise.toml pins are NOT covered here — Dependabot has no mise ecosystem yet
# (dependabot-core#12320). See .github/workflows/mise-upgrade.yml for weekly bumps.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    groups:
      github-actions:
        patterns:
          - "*"
    cooldown:
      default-days: 3

  - package-ecosystem: gitsubmodule
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 3
    groups:
      bats-submodules:
        patterns:
          - "*"
