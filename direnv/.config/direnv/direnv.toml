# ~/.config/direnv/direnv.toml

[global]
# Warn if loading an .envrc takes longer than this.
warn_timeout = "5s"
# Don't print the (often noisy) environment diff on every dir change.
hide_env_diff = true
